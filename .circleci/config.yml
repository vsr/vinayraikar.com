version: 2
jobs:
  build:
    docker:
      - image: jguyomard/hugo-builder:latest

    working_directory: ~/repo
    steps:
      - checkout
      - run: ./run clean
      - run: ./run build
      - run: ./run deploy
